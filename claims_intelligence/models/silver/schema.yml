version: 2

sources:
  - name: part_d_raw
    database: claims-intelligence
    schema: raw
    freshness:
      warn_after: {count: 1, period: day}
      error_after: {count: 2, period: day}
    tables:
      - name: raw_data
        loaded_at_field: loaded_at

models:
  - name: part_d_silver
    columns:
      - name: prescriber_npi
        tests:
          - not_null
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - prescriber_npi
            - data_year

